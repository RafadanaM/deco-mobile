import 'package:flutter/material.dart';

class Test extends StatelessWidget {
  static const routeName = '/mantap';
  const Test({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Mantap"),
    );
  }
}
